services:
  pyspark:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: clinical-trial-pyspark
    volumes:
      - ..:/app
    working_dir: /app
    environment:
      - PYTHONPATH=/app
    command: tail -f /dev/null